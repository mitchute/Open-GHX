language: python
python:
  - "3.6"

cache: pip

install:
  - "pip install -r requirements.txt"
  - pip install coveralls

script:
  - coverage run --source=ghx test_ghx.py

after_success:
  coveralls
